select name as Customers from Customers
left join Orders on Orders.customerId = Customers.id where Orders.id is null;


#-------------------------
select name as Customers from Customers
where id not in (select customerId from Orders);
